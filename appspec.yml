version: 0.0
os: linux
files:
  - source: /aws
    destination: /usr/local/codedeployresources

hooks:
  ApplicationStop:
    - location: scripts/stop_application.sh
      timeout: 300
      runas: root
